package exc_testcode;

/*
4. 정수 array를 멤버변수로 가지면서 아래 요구 사항을 만족하는 클래스 MyArray와 이를 테스트하는 클래스
TestMyArray를 작성하시오. (java.util.Arrays
등 다른 클래스를 사용해서는 안 됨.
 ) [30점]
 1) 멤버변수는 외부에서 접근 불가능 (뒤에 정의되는 get 메소드로만 접근).
 2) object를 생성할 때, 정수형 size를 주면 그 값만큼의 정수 element를 갖는 array를 생성하고, 정수형 array를 주
면 같은 크기와 element 값들을 갖는 array를 생성한다.
 3) element 하나 값 또는
array 값 전체를 return하는 get 메소드 포함. (두 경우 모두 보여야 함.
 )
 4) 주어진 MyArray object와 같은 지 비교하는 equals 메소드 포함. (대응하는 각각의 element들이 모두 같으면 참.)
 5) 인덱스 from부터 인덱스 to까지 element 중에서 최대값을 갖는 element의 인덱스
를 알려주는 maxI(from, to) 포함
하며,
 6) maxI(from, to) 를 이용해서 array element 중 최대값
을 return하는 max()도 포함.
 7) 지금까지 생성한 MyArray object의 총 개수를 알려주는 numOfArrayCreated() 포함.
 8) TestMyArray에는
1) {1,5,3,9,7}에 해당하는 array로 MyArray object를 생성하고,
 2) get으로 3번째 element ‘3’ 값을 가져오며,
 3) {2,4}로 MyArray object를 하나 더 생성하고,
 4) numOfArrayCreated() 확인하며,
 5) 두 object가 같은지 equals 테스트
print문을 신경 쓸 필요 없음
. 테스트하는 문장만 있으면 됨
*/
public class test4 {

	private int getElement()
	
}
